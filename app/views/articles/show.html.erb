<div>
  <div>
    <h1><%= @article.title %></h1>
    <%= link_to "Edit this article", edit_article_path(@article) %> |
    <%= link_to "Delet this article", article_path(@article), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
  </div>

  <div>
    <p><%= @article.content %></p>
  </div>

</div>
